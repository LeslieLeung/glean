import ReactDOM from 'react-dom/client'

const root = document.getElementById('root')
if (root) {
  ReactDOM.createRoot(root).render(
    <div>
      <h1>Hello from React!</h1>
      <p>Simple test without routing</p>
    </div>
  )
}
